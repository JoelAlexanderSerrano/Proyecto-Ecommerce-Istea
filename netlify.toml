[build]
  # Comando de construcción (si no tienes uno, es buena práctica incluirlo)
  # command = "npm install" 
  publish = "." # Carpeta que se publicará (la raíz del proyecto)

[functions]
  # 🚨 ESTO ES CLAVE: Le dice a Netlify que busque funciones en la carpeta 'api' 🚨
  directory = "api"